package com.playfab.<%- api.name.toLowerCase() %>models;

<% if(needsPlayFabMap) { %>import com.playfab.PlayFabMap;
<% } %><% if(needsPlayFabUtil) { %>import com.playfab.PlayFabUtil;

<% } %>class <%- datatype.name %>
{
<% for(var i in datatype.properties) { var property = datatype.properties[i] %>	<%- getPropertyDef(property, datatype) %>
<% } %>	
	public function new(?data:Dynamic)
	{
		if(data == null)
			return;
		
<% for(var i in datatype.properties) { var property = datatype.properties[i] %>		<%- getPropertyInit(property, datatype) %>
<% } %>	}
}
